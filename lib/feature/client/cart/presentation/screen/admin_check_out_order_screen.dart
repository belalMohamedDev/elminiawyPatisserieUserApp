import 'package:flutter/material.dart';

class AdminCheckOutOrderScreen extends StatelessWidget {
  const AdminCheckOutOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
